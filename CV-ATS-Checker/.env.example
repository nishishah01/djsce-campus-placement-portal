# Allowed CORS origins (comma-separated). Example for local React/Next frontend
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:8080/
# Maximum JD keywords considered in scoring (to prevent tiny-noise effects)
MAX_JD_KEYWORDS=400
# Weights for scoring (must sum ~1.0)
WEIGHT_KEYWORDS=0.65
WEIGHT_COMPLETENESS=0.2
WEIGHT_FORMAT=0.15

# uvicorn main:app --port 8081 --reload